# TungBox TODO

最后更新：2026-06-02 | 当前版本 v0.1.2(0053)

TungBox 是面向 macOS 的 sing-box 图形客户端，目标是为日常代理需求提供开箱即用的体验。

## 已完成 ✅

- ✅ **`P0`** 首次启动零状态：无订阅时侧边栏红点 + 订阅页空状态引导文案
- ✅ **`P0`** 退出清理链路：同步关闭系统代理、停止 sing-box、TUN Daemon 仅停子进程不误卸载
- ✅ **`P0`** TUN 全链路：LaunchDaemon 安装/卸载/启用/禁用，启动时崩溃恢复
- ✅ **`P0`** Core 更新链路：四种场景错误提示 + 下载超时 + 导入验证
- ✅ **`P0`** 应用图标：NSWorkspace.setIcon 双层修复（打包时 + 首次启动）
- ✅ **`P0`** 订阅兼容性检查：自动检测弃用字段，可自动修复的静默处理 + 日志输出，需手动的弹窗建议降级 Core
- ✅ **`P1`** Clash YAML 订阅导入：自动识别机场通用格式并转换为 sing-box 配置
- ✅ **`P1`** 订阅刷新间隔可配置 + 失败通知 + 卡片错误状态
- ✅ **`P1`** 导入方式多样化：URL 输入 / 本地文件 / 剪贴板
- ✅ **`P1`** 连接页增强：按节点/域名/IP/规则过滤搜索 + 右键关闭单条/全部连接 + 单条实时速率
- ✅ **`P1`** 自定义规则增强：添加 / 编辑 / 启用禁用 / 删除
- ✅ **`P1`** 日志增强：按级别过滤 + 关键词搜索 + 行数统计 + 复制导出
- ✅ **`P1`** URLTest 断连：`interrupt_exist_connections: true` + `closeConnections()` on switch
- ✅ **`P1`** 节点切换一致性：UI / 状态栏 / API / 配置文件四源同一 `selectNode()` 入口
- ✅ **`P1`** 规则集缓存管理：手动刷新 + 清空缓存
- ✅ 左侧一级菜单 + MD3 风格主界面 + 深浅色主题
- ✅ 首页仪表盘：系统代理/TUN/出站模式切换，实时速率、连接数、URLTest 延迟
- ✅ 节点页：代理分组网格、URLTest 延迟测试、自动选择最快节点
- ✅ 运行控制：sing-box 启动/停止、配置保存/检查/格式化
- ✅ Core 管理：检测、导入、自动安装最新版/旧版、更新检查（自动暂停代理）
- ✅ 状态栏 + 后台运行 + 开机自启动 & 静默启动
- ✅ 发布打包：自动编译瘦身版 Core → .app 签名 → .dmg

## P2 — 体验增强

- [x] **代理冲突检测**：启动时 `lsof` 扫描常见代理端口，弹窗提示关闭冲突软件。
- [x] **窗口状态持久化**：记住窗口大小、位置、上次打开的 Tab，下次启动恢复。
- [x] **主题持久化**：深浅色切换写入 UserDefaults，重启不丢失。
- [x] **流量统计持久化**：今日 / 近 7 天 / 近 30 天流量写入 UserDefaults，重启后仍在。

## P3 — 工程化 & 测试

- [x] **核心逻辑单测**：21 个 XCTest 覆盖格式检测、Clash YAML 解析、版本比较、兼容性检查、订阅导入，全通过。
- [x] **打包回归**：`package_app.sh` → build + Core + icon + sign + DMG 全链跑通。

---

## 发布 Checklist（每次 Release 前核对）

- [ ] `AppMetadata.swift` 中版本号 + Build 号已更新
- [ ] `script/package_app.sh` 跑通，DMG + SHA256 产出于 `dist/`
- [ ] 在干净 macOS 上安装 .app → Core 自动就绪 → 首次启动流程走通
- [ ] GitHub Release 标题 = `v{version}`，Body 含安装说明
- [ ] `README.md` 版本号同步更新
